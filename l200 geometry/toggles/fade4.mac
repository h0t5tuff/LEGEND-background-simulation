# FADE 4/17 (hide/cull): WLSR copper shell (1 vols).
# Hides this component (culled from the render) -> reveals the WLSR tetratex lining.
# Run fade1..fade17 in order to melt the array away from outside in.
/vis/geometry/set/visibility wlsr_copper 0 false
